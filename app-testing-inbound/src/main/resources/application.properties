# Inbound Lambda — environment-driven configuration
# Values are overridden by Lambda environment variables in AWS / LocalStack

aws.region=${AWS_REGION:us-east-1}
aws.sqs.queue.url=${SQS_QUEUE_URL:}
aws.sqs.endpoint=${SQS_ENDPOINT:}

# Logging
logging.level.com.atheor=INFO
